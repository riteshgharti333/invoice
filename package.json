{
  "name": "invoice-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "packages/shared"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "dev:fontend": "npm run dev --workspace=frontend",

    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
{
  "name": "invoice-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "packages/shared"
  ],
  "scripts": {
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}